import 'package:flutter/material.dart';

class TeamPickScreeen extends StatelessWidget {
  const TeamPickScreeen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container());
  }
}
